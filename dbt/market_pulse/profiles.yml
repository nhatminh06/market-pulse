market_pulse:
  target: dev
  outputs:
    dev:
      type: trino
      host: trino
      port: 8080
      user: admin
      database: iceberg     # the Trino catalog
      schema: silver        # default schema; gold models override via +schema
      threads: 4
      http_scheme: http